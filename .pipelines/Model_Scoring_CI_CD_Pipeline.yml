# Pipeline to run basic code quality tests as part of pull requests.
  trigger: none
  
  pool:
    name: 'Azure Pipelines'
    vmImage: 'ubuntu-16.04'
  
  jobs:
    - job: "Model_Scoring_CI_CD_Pipeline"
      displayName: "Model Scoring CI CD Pipeline"
      steps:
      # Run checks
      - task: Bash@3
        displayName: Hello World
        inputs:
          targetType: 'inline'
          workingDirectory: ./code/mlops/model-scoring
          script: |
            echo 'Hello World' 
  